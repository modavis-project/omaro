{
  "name": "omaro-field-guide",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A standalone, progressively disclosed guide to OMARO",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "node scripts/build.mjs",
    "dev": "node scripts/serve.mjs",
    "test": "node --test tests/*.test.mjs",
    "check": "node --check src/app.js && node --check src/model.js && node --check src/content.js"
  }
}
